# main
main(): repl-loop("")

# read
READ(strng): strng

# eval
EVAL(ast): ast

# print
PRINT(exp): exp

# repl
rep(strng): PRINT((EVAL (READ strng)))

# repl loop
repl-loop(line):
  if line:
  - - if not((= "" line)):
      - println((rep line)))
    - repl-loop((readline "mal-user> "))
